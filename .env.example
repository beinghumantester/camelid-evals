# Where the live Camelid instance is reachable.
CAMELID_BASE_URL=http://127.0.0.1:8181

# Only needed for the "semantic" domains (evidence-audit, model-compatibility),
# which run a real skill via Claude and judge its answer. self-conformance and
# regression-museum never touch this key.
ANTHROPIC_API_KEY=
